# vault-mind profile

<!--
Generated by /vault-mind:init. Every vault-mind skill reads this file to learn THIS vault's
shape. Edit by hand or re-run /vault-mind:init. Keep paths relative to the vault root.
-->

## Paths

- **vault_root:** `.`
- **notes_dir:** `Notes/`                       <!-- atomic/concept notes (the Zettelkasten) -->
- **resources_dirs:**                            <!-- reading material; map each to a class. omit if no reading log -->
  - `Resources/Books/` → Book
  - `Resources/Essays/` → Essay
  - `Resources/Papers/` → Paper
- **assets_dir:** `Assets/`                       <!-- images + fetched sources (used by /vault-mind:fetch) -->
- **templates_dir:** `Templates/`                <!-- optional -->

## Concept-note frontmatter

| Role | Key in this vault |
|------|-------------------|
| tags | `tags` |
| related links | `related` |
| source readings | `resources` |
| created date | `date` |
| review date (for /vault-mind:quiz spacing) | `reviewed` |
| audited date (for /vault-mind:audit incremental skip) | `audited` |

- **tag style:** hierarchical (`a/b` ⊂ `a`)      <!-- or: flat -->
- **wikilink style:** `[[Title]]`                <!-- or: [[path/Title]] | plain markdown links -->

## Reading frontmatter

| Role | Key | Values / scale |
|------|-----|----------------|
| title | `title` |, |
| author | `author` | wikilink(s) to a person note |
| status | `status` | `read` \| `reading` \| `to be read` \| `DNF` |
| rating | `rating` | 0–10 (decimal, quoted); or `DNF`; blank if unrated |
| tags | `tags` | hierarchical |
| online source | `url` |, |
| local copy | `local` | wikilink to fetched file (set by /vault-mind:fetch) |

## Conventions

- **methodology:** Zettelkasten (atomic notes, one idea each, densely wikilinked)
- **what "read" signal means:** `status: read`/`reading` = consumed; `to be read` = backlog;
  `DNF` = abandoned (negative signal).
- **taste signal:** high `rating` (≈ top quartile of the scale) reveals what the user values.
- **publishing:** <!-- e.g. a Quartz/Publish subset, or none -->
